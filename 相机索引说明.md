# 相机索引（Camera Index）说明

## 什么是相机索引？

**相机索引**是 OpenCV 用来标识和访问相机的**数字编号**。就像给每个相机分配一个号码，程序通过这个号码来找到并使用对应的相机。

## 为什么需要索引？

当您的电脑连接了多个相机（比如笔记本内置摄像头、USB 外接摄像头等）时，系统需要一种方式来区分它们。索引就是这种区分方式。

## 不同操作系统的差异

### Windows / macOS
- 索引通常是连续的：0, 1, 2, 3...
- 第一个相机通常是 0，第二个是 1，以此类推
- 相对稳定，重启后变化较小

### Linux（如 Ubuntu）
- 索引可能不连续：6, 16, 23...
- 这是因为 Linux 系统会为所有视频设备（包括虚拟设备）分配设备节点
- **索引可能变化**：重启电脑或更换 USB 端口后，索引可能会改变

## 在您的系统中

根据测试结果，您的相机索引是 **6**：

```
✅ 相机 6 可用！
   分辨率: 640x480
```

这意味着：
- 您的相机在系统中被分配为索引 6
- 使用 OpenCV 访问时，需要指定 `index_or_path=6`

## 如何使用索引？

### 方法 1：使用数字索引（推荐用于 Windows/macOS）

```python
from lerobot.cameras.opencv import OpenCVCameraConfig

cameras = {
    "webcam": OpenCVCameraConfig(
        index_or_path=6,  # ← 使用索引 6
        fps=30,
        width=640,
        height=480,
    )
}
```

### 方法 2：使用设备路径（推荐用于 Linux，更稳定）

在 Linux 系统中，使用设备路径比索引更稳定：

```python
cameras = {
    "webcam": OpenCVCameraConfig(
        index_or_path="/dev/video6",  # ← 使用设备路径
        fps=30,
        width=640,
        height=480,
    )
}
```

## 如何查找相机索引？

### 方法 1：使用 LeRobot 工具（推荐）

```bash
lerobot-find-cameras opencv
```

输出示例：
```
--- Detected Cameras ---
Camera #0:
  Name: OpenCV Camera @ 6
  Type: OpenCV
  Id: 6
  Default stream profile:
    Width: 640
    Height: 480
    Fps: 30.0
```

### 方法 2：查看 Linux 设备文件

```bash
ls -l /dev/video*
```

会显示所有视频设备：
```
/dev/video0
/dev/video1
...
/dev/video6  ← 您的相机
...
```

### 方法 3：运行测试脚本

```bash
python test_if_camera.py
```

## 索引可能变化的情况

⚠️ **重要**：在 Linux 系统中，相机索引可能会变化：

1. **重启电脑后** - 索引可能改变
2. **更换 USB 端口后** - 索引可能改变
3. **连接/断开其他 USB 设备后** - 索引可能改变

### 解决方案

**方案 1：使用设备路径（更稳定）**
```python
index_or_path="/dev/video6"  # 而不是 index_or_path=6
```

**方案 2：每次启动前查找**
```bash
# 每次使用前运行
lerobot-find-cameras opencv
# 然后更新配置中的索引
```

**方案 3：使用 udev 规则固定设备路径**（高级）
可以为特定相机创建固定的符号链接，这样设备路径就不会变化。

## 总结

- **相机索引** = OpenCV 用来标识相机的数字编号
- 您的相机索引是 **6**
- Linux 系统中索引可能变化，建议使用设备路径 `/dev/video6` 而不是数字 6
- 使用 `lerobot-find-cameras opencv` 可以查找当前可用的相机索引

## 在机器人配置中使用

```python
from lerobot.cameras.opencv import OpenCVCameraConfig

@RobotConfig.register_subclass("your_robot")
@dataclass
class YourRobotConfig(RobotConfig):
    cameras: dict[str, CameraConfig] = field(
        default_factory=lambda: {
            "webcam": OpenCVCameraConfig(
                index_or_path=6,  # 或使用 "/dev/video6"
                fps=30,
                width=640,
                height=480,
            ),
        }
    )
```

