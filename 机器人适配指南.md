# LeRobot 机器人适配指南

本指南将详细说明如何将您自己的机器人适配到 LeRobot 框架中，以便使用 LeRobot 的数据采集功能。

## 📋 目录

1. [概述](#概述)
2. [适配流程概览](#适配流程概览)
3. [详细步骤](#详细步骤)
4. [数据采集流程](#数据采集流程)
5. [测试与验证](#测试与验证)
6. [常见问题](#常见问题)

---

## 概述

### 什么是机器人适配？

机器人适配是指为您的机器人创建一个符合 LeRobot 接口规范的实现类，使得您的机器人能够：
- 使用 LeRobot 的数据采集工具
- 与 LeRobot 的遥操作设备配合工作
- 使用 LeRobot 的策略训练和推理功能

### 核心接口

LeRobot 定义了一个 `Robot` 抽象基类，所有机器人实现都需要继承这个类并实现其抽象方法。这个接口标准化了：
- **观察（Observation）**：从机器人读取传感器数据
- **动作（Action）**：向机器人发送控制命令
- **连接管理**：建立和断开与硬件的连接
- **校准**：机器人校准功能（如需要）

---

## 适配流程概览

### 整体流程图

```
┌─────────────────────────────────────────────────────────────┐
│  1. 准备工作                                                 │
│     - 了解机器人硬件接口                                     │
│     - 准备通信库/SDK                                        │
│     - 确定观察和动作空间                                     │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│  2. 创建配置文件                                            │
│     - 创建 Config 类（继承 RobotConfig）                     │
│     - 定义机器人参数（IP、端口、相机等）                     │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│  3. 实现机器人类                                             │
│     - 继承 Robot 基类                                        │
│     - 实现所有抽象方法（10个）：                             │
│       • 2个类属性：config_class, name                       │
│       • 4个属性：observation_features, action_features,    │
│         is_connected, is_calibrated                         │
│       • 6个方法：connect, disconnect, calibrate, configure,  │
│         get_observation, send_action                        │
│     - 实现硬件通信逻辑                                       │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│  4. 注册机器人类型                                           │
│     - 在 utils.py 中注册                                     │
│     - 在 __init__.py 中导出                                  │
└─────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│  5. 测试与验证                                               │
│     - 测试连接功能                                           │
│     - 测试数据采集                                           │
│     - 验证数据格式                                           │
└─────────────────────────────────────────────────────────────┘
```

### 需要创建的文件

```
src/lerobot/robots/your_robot/
├── __init__.py          # 导出配置和机器人类
├── config_your_robot.py # 机器人配置类
└── your_robot.py        # 机器人实现类
```

---

## 详细步骤

### 步骤 1: 准备工作

在开始编码之前，您需要：

#### 1.1 了解您的机器人硬件

- **通信接口**：机器人使用什么通信方式？
  - 串口（Serial/USB）、网络（TCP/IP, UDP）、CAN 总线、其他协议

- **传感器**：机器人有哪些传感器？
  - 关节编码器（位置、速度）、相机、力/力矩传感器、其他传感器

- **执行器**：机器人如何接收控制命令？
  - 关节位置控制、关节速度控制、关节力矩控制、末端执行器控制

#### 1.2 准备通信库

根据您的机器人类型，您可能需要：

- **使用 LeRobot 内置的电机总线**（如果适用）：
  - `FeetechMotorsBus` - 用于 Feetech 舵机
  - `DynamixelMotorsBus` - 用于 Dynamixel 舵机

- **使用第三方 SDK**：
  - 机器人厂商提供的 Python SDK
  - 或使用 C/C++ 绑定的 Python 包装器

- **实现自定义通信**：
  - 使用 `pyserial` 进行串口通信
  - 使用 `socket` 进行网络通信
  - 使用 `canopen` 等进行 CAN 总线通信

#### 1.3 确定数据格式

明确您的机器人的：
- **观察空间（Observation Space）**：包含哪些数据？格式是什么？
- **动作空间（Action Space）**：接受哪些控制命令？格式是什么？

---

### 步骤 2: 创建配置文件

#### 2.1 创建配置类

在 `src/lerobot/robots/your_robot/config_your_robot.py` 中创建配置类：

- 继承 `RobotConfig` 基类
- 使用 `@RobotConfig.register_subclass("your_robot")` 装饰器注册机器人类型
- 定义机器人连接参数（如 `robot_ip`、`port` 等）
- 定义相机配置（如果有相机）
- 类型名称 `"your_robot"` 将用于命令行参数 `--robot.type=your_robot`

**关键点**：
- 所有需要配置的参数都应该在这里定义
- 相机配置使用 `CameraConfig` 类型，支持 OpenCV、RealSense 等

---

### 步骤 2.5: 关于相机适配（可选）

**重要说明**：大多数情况下，您**不需要**自己实现相机类！

LeRobot 已经提供了多种相机实现，可以直接使用：
- **OpenCVCamera** - 适用于大多数 USB 相机（通过 OpenCV 访问）
- **RealSenseCamera** - 适用于 Intel RealSense 相机
- **Reachy2Camera** - 适用于 Reachy2 机器人的相机

**只有在以下情况下才需要自己实现相机类**：
- 您的相机使用特殊的驱动或协议
- 需要自定义相机行为（如特殊的图像处理）
- 相机无法通过 OpenCV 或现有实现访问

#### 如果需要实现自定义相机类

相机也需要继承抽象基类 `Camera`，需要实现以下抽象方法：

1. **`is_connected`** (属性) - 检查相机是否已连接
2. **`find_cameras()`** (静态方法) - 查找系统中可用的相机
3. **`connect(warmup: bool = True)`** - 连接相机
4. **`read(color_mode: ColorMode | None = None)`** - 同步读取一帧图像
5. **`async_read(timeout_ms: float = ...)`** - 异步读取一帧图像
6. **`disconnect()`** - 断开相机连接

同时还需要：
- 创建相机配置类（继承 `CameraConfig`）
- 在 `make_cameras_from_configs()` 函数中注册您的相机类型

**建议**：优先使用现有的相机实现，只有在确实需要时才自己实现。

---

### 步骤 3: 实现机器人类

#### 3.0 需要实现的抽象方法清单

在实现机器人类之前，您需要了解 `Robot` 基类要求实现的所有抽象方法和属性。以下是完整的清单：

**必须设置的类属性**（在类定义中直接赋值）：
- `config_class`: 配置类的类型，例如 `YourRobotConfig`
- `name`: 机器人类型的字符串标识符，例如 `"your_robot"`

**必须实现的抽象属性**（使用 `@property` 装饰器）：
1. **`observation_features`** - 定义观察数据的结构和类型
   - 返回：`dict`，键为观察名称，值为类型（如 `float`）或形状元组（如 `(480, 640, 3)`）
   - 必须在未连接时也能调用

2. **`action_features`** - 定义动作数据的结构和类型
   - 返回：`dict`，键为动作名称，值为类型（如 `float`）
   - 必须在未连接时也能调用

3. **`is_connected`** - 检查机器人是否已连接
   - 返回：`bool`

4. **`is_calibrated`** - 检查机器人是否已校准
   - 返回：`bool`
   - 如果机器人不需要校准，始终返回 `True`

**必须实现的抽象方法**：
5. **`connect(calibrate: bool = True)`** - 建立与机器人的连接
   - 参数：`calibrate` - 是否在连接后自动校准
   - 返回：`None`
   - 功能：初始化硬件连接、连接相机、配置机器人、执行校准（如需要）

6. **`disconnect()`** - 断开与机器人的连接
   - 返回：`None`
   - 功能：关闭所有连接、释放资源、清理状态

7. **`calibrate()`** - 校准机器人（如果适用）
   - 返回：`None`
   - 功能：执行校准流程、更新 `self.calibration` 字典、保存校准数据
   - 如果机器人不需要校准，可以留空（`pass`）

8. **`configure()`** - 配置机器人参数
   - 返回：`None`
   - 功能：设置控制模式、PID参数、电机参数等
   - 通常在连接时调用一次

9. **`get_observation()`** - 读取当前观察数据
   - 返回：`dict[str, Any]`，键必须与 `observation_features` 匹配
   - 功能：读取关节状态、相机图像、传感器数据等
   - 如果未连接，应抛出 `DeviceNotConnectedError`

10. **`send_action(action: dict[str, Any])`** - 发送动作命令
    - 参数：`action` - 动作字典，键必须与 `action_features` 匹配
    - 返回：`dict[str, Any]` - 实际发送的动作（可能被限幅或修改）
    - 功能：将动作命令发送到硬件、应用安全限制、返回实际执行的动作
    - 如果未连接，应抛出 `DeviceNotConnectedError`

**总结**：您需要实现 **10 个抽象成员**（2个类属性 + 4个属性 + 6个方法）

#### ✅ 快速检查清单

在完成机器人类实现后，请确认以下**10个抽象成员**都已实现：

- ✅ 类属性：`config_class`、`name`
- ✅ 抽象属性：`observation_features`、`action_features`、`is_connected`、`is_calibrated`
- ✅ 抽象方法：`connect()`、`disconnect()`、`calibrate()`、`configure()`、`get_observation()`、`send_action()`

**验证方法**：
- 如果缺少任何一个抽象成员，Python 会在实例化时抛出 `TypeError`
- 可以使用 `YourRobot.__abstractmethods__` 检查，应该返回空集合 `set()`
- 尝试实例化：`robot = YourRobot(config)` 应该不会报错（除了连接相关的错误）

#### 3.1 创建机器人类框架

在 `src/lerobot/robots/your_robot/your_robot.py` 中：

- 继承 `Robot` 基类
- 设置 `config_class` 和 `name` 类属性
- 在 `__init__` 中初始化配置、硬件连接对象、相机对象

#### 3.2 定义观察和动作特征

- **`observation_features`**：定义观察数据的结构
  - 包含关节位置/速度（如 `"joint_1.pos": float`）
  - 包含相机图像（如 `"webcam": (480, 640, 3)`）
  - 键必须与 `get_observation()` 返回的字典键完全匹配

- **`action_features`**：定义动作数据的结构
  - 通常包含关节目标位置（如 `"joint_1.pos": float`）
  - 可能包含末端执行器控制（如 `"gripper.pos": float`）
  - 键必须与 `send_action()` 接受的字典键完全匹配

**重要提示**：
- 这些属性必须在机器人未连接时也能调用
- 键名必须完全匹配（包括大小写）

#### 3.3 实现连接管理

- **`is_connected`**：检查机器人硬件连接状态
- **`is_calibrated`**：检查校准状态（如不需要校准，返回 `True`）
- **`connect()`**：
  - 建立硬件连接（根据您的SDK调用相应方法）
  - 连接所有相机
  - 调用 `configure()` 配置机器人
  - 如需要，调用 `calibrate()` 进行校准
- **`disconnect()`**：
  - 断开所有相机连接
  - 断开机器人硬件连接
  - 清理资源

#### 3.4 实现校准和配置

- **`calibrate()`**：
  - 如果机器人不需要校准，直接 `pass`
  - 如果需要，实现校准逻辑（记录零位、范围等）
  - 更新 `self.calibration` 字典
  - 调用 `self._save_calibration()` 保存校准数据

- **`configure()`**：
  - 设置机器人控制模式
  - 配置PID参数、电机参数等
  - 通常在连接时调用一次

#### 3.5 实现观察读取

**`get_observation()`** 是数据采集的核心方法：

1. 检查连接状态，如未连接抛出异常
2. 读取关节状态（位置、速度等），转换为观察字典
3. 读取相机图像：
   - 使用 `cam.async_read()` 异步读取
   - 处理读取失败的情况（返回黑色图像）
   - 确保图像格式为 numpy 数组，形状 (H, W, 3)，类型 uint8
4. 返回完整的观察字典

**关键点**：
- 返回的字典键必须与 `observation_features` 完全匹配
- 图像必须是 RGB 格式的 uint8 numpy 数组
- 处理异常情况，避免程序崩溃

#### 3.6 实现动作发送

**`send_action()`** 是控制机器人的核心方法：

1. 检查连接状态，如未连接抛出异常
2. 验证动作字典，确保所有必需的键都存在
3. 提取关节目标位置/速度
4. 发送命令到硬件（根据您的SDK调用相应方法）
5. 如有末端执行器，处理末端执行器控制
6. 返回实际发送的动作（可能被限幅或修改）

**关键点**：
- 可以在这里添加安全限制（速度限制、位置限制等）
- 返回实际发送的动作，而不是输入的动作

---

### 步骤 4: 注册机器人类型

#### 4.1 在 `__init__.py` 中导出

在 `src/lerobot/robots/your_robot/__init__.py` 中导出配置类和机器人类。

#### 4.2 在 `utils.py` 中注册

在 `src/lerobot/robots/utils.py` 的 `make_robot_from_config` 函数中添加您的机器人类型判断。

#### 4.3 在 `record.py` 中导入（可选）

如果要在命令行中使用，在 `src/lerobot/record.py` 中添加导入语句。

---

## 数据采集流程

### LeRobot 数据采集的工作原理

LeRobot 的数据采集遵循以下流程：

1. **初始化阶段**：创建机器人对象、数据集对象、遥操作设备（可选）
2. **连接阶段**：调用 `robot.connect()` 和 `teleop.connect()`（如果使用）
3. **数据采集循环**（每个 Episode）：
   - 重置环境（可选）
   - 循环（直到 `episode_time_s`）：
     - `observation = robot.get_observation()`
     - `action = teleop.get_action()` 或从策略获取
     - `robot.send_action(action)`
     - `dataset.append(observation, action)`
4. **保存和上传**：保存数据集到本地，上传到 HuggingFace Hub（可选）

### 数据格式要求

LeRobot 数据集使用以下格式：
- `observation`：包含所有观察数据（关节状态、相机图像等）
- `action`：包含所有动作数据（关节目标位置等）
- `episode_index`：每个帧属于哪个episode
- `frame_index`：帧在episode中的索引
- `timestamp`：时间戳

### 使用命令行采集数据

使用 `lerobot-record` 命令，指定机器人类型、连接参数、数据集配置等。

### 使用 Python API 采集数据

1. 创建配置对象
2. 创建机器人、遥操作设备对象
3. 创建数据集对象
4. 连接所有设备
5. 调用 `record_loop()` 进行数据采集
6. 断开所有连接

---

## 测试与验证

### 测试步骤

#### 1. 测试连接
- 创建机器人对象
- 调用 `connect()` 测试连接
- 检查 `is_connected` 和 `is_calibrated` 状态
- 调用 `disconnect()` 测试断开

#### 2. 测试观察读取
- 连接机器人
- 调用 `get_observation()` 读取观察
- 验证观察键与 `observation_features` 匹配
- 验证数据格式（标量类型、图像形状等）

#### 3. 测试动作发送
- 连接机器人
- 创建测试动作字典
- 调用 `send_action()` 发送动作
- 验证机器人响应

#### 4. 测试数据采集
- 运行一个简短的采集测试
- 验证数据集格式正确

### 常见问题排查

#### 问题 1: 连接失败
- **可能原因**：IP地址或端口错误、网络连接问题、机器人未启动、权限问题
- **解决方法**：检查网络连接、检查串口权限、检查机器人状态

#### 问题 2: 观察读取失败
- **可能原因**：SDK调用错误、数据格式不匹配、相机未连接
- **解决方法**：添加异常处理和日志、验证数据格式、检查相机连接状态

#### 问题 3: 动作发送无效
- **可能原因**：动作值超出范围、控制模式不正确、机器人处于安全模式
- **解决方法**：添加值范围检查、验证控制模式、检查机器人状态

#### 问题 4: 数据格式不匹配
- **可能原因**：`observation_features` 与 `get_observation()` 返回的键不匹配、图像格式不正确
- **解决方法**：确保键完全匹配、验证图像形状和数据类型、使用测试代码验证

---

## 常见问题

### Q1: 我的机器人使用自定义通信协议，怎么办？

A: 您需要实现自己的通信层。可以使用 `pyserial` 进行串口通信、`socket` 进行网络通信，或其他适合您协议的库。

### Q2: 我的机器人有多个末端执行器，如何处理？

A: 在 `action_features` 和 `observation_features` 中为每个末端执行器添加相应的键，例如 `"left_gripper.pos"` 和 `"right_gripper.pos"`。

### Q3: 我的机器人需要特殊的校准流程，怎么办？

A: 在 `calibrate()` 方法中实现您的校准逻辑。可以参考其他机器人的实现，例如使用交互式提示让用户移动机器人到特定位置。

### Q4: 如何添加力/力矩传感器数据？

A: 在 `observation_features` 中添加力/力矩特征（如 `"force_sensor.fx"`、`"force_sensor.fy"`、`"force_sensor.fz"`），然后在 `get_observation()` 中读取并返回这些值。

### Q5: 我的机器人使用不同的控制模式（位置/速度/力矩），如何切换？

A: 在 `configure()` 或 `send_action()` 中根据动作类型切换控制模式。也可以为不同的动作类型定义不同的特征。

### Q6: 如何优化数据采集的性能？

A: 使用异步相机读取、优化硬件通信（批量读取、减少延迟）、调整图像分辨率、使用多线程处理。

### Q7: 我的机器人需要安全限制，如何实现？

A: 在 `send_action()` 中添加安全检查，例如位置限幅、速度限制、加速度限制等。

### Q8: 我需要自己实现相机类吗？

A: **大多数情况下不需要**。LeRobot 已经提供了 OpenCVCamera（适用于大多数 USB 相机）、RealSenseCamera 等实现。只有在您的相机使用特殊驱动或协议，无法通过现有实现访问时，才需要自己实现相机类。相机也需要继承 `Camera` 抽象基类，实现 `is_connected`、`find_cameras()`、`connect()`、`read()`、`async_read()`、`disconnect()` 等方法。

---

## 参考资源

### 示例实现

- **UR5e**: `src/lerobot/robots/ur5e/` - 网络连接的工业机械臂
- **LeKiwi**: `src/lerobot/robots/lekiwi/` - 移动机械臂
- **SO100**: `src/lerobot/robots/so100_follower/` - 使用 Feetech 舵机的机械臂

### 相关文档

- [LeRobot 官方文档 - 硬件集成](https://github.com/huggingface/lerobot/blob/main/docs/source/integrate_hardware.mdx)
- [相机集成指南](./cameras.mdx)
- [遥操作设备集成](./teleoperators.mdx)

### 获取帮助

- [LeRobot Discord 社区](https://discord.gg/s3KuuzsPFb)
- [GitHub Issues](https://github.com/huggingface/lerobot/issues)

---

## 总结

适配您的机器人到 LeRobot 框架需要以下关键步骤：

1. ✅ **创建配置类** - 定义机器人参数
2. ✅ **实现机器人类** - 继承 `Robot` 基类并实现所有抽象方法（10个）
3. ✅ **注册机器人类型** - 在 `utils.py` 中注册
4. ✅ **测试验证** - 确保所有功能正常工作
5. ✅ **数据采集** - 使用 LeRobot 工具采集数据

遵循本指南，您应该能够成功地将您的机器人集成到 LeRobot 生态系统中。如果在适配过程中遇到问题，请参考示例实现或向社区寻求帮助。

祝您适配顺利！🚀
